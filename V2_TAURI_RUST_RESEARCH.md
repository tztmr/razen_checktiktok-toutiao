# 第二版研究方案：Tauri + Rust

本文档用于定义第二版的目标、边界、能力拆分、技术重点和推荐实施顺序。

当前前提：

- 第一版已经具备基础桌面壳
- 已支持 `ZIP` 扫描
- 已支持 `plist / json / sqlite` 基础解析
- 已具备最小 UI 工作流

第二版不再以“先跑通”为目标，而是以“可持续使用、可演示、可发布”为目标。

## 1. 第二版目标

第二版核心目标有 5 个：

1. 把第一版从“技术原型”升级成“可用工具”
2. 补齐导出、缓存、错误提示和稳定性
3. 为 `mmkv` 解析打基础
4. 为双平台发布和 CI 打包打通标准流程
5. 为后续批量分析和更复杂数据查看保留架构空间

## 2. 第二版不追求什么

第二版仍然不建议直接做这些高复杂度能力：

- 不做云同步
- 不做多人协作
- 不做在线数据库
- 不做脚本市场或自动化平台
- 不做完整反序列化所有私有二进制格式

第二版依然聚焦“本地 ZIP 分析器”。

## 3. 第二版建议范围

推荐把第二版拆成 6 个模块。

### 3.1 导出能力

第一版只是界面里看结果，第二版应补齐：

- 导出当前文件解析结果
- 导出当前 APP 全部解析结果
- 导出本次 ZIP 的扫描摘要
- 导出统一 JSON

如果时间允许，可增加：

- `sqlite` 表预览单独导出
- 解析失败记录导出

### 3.2 缓存能力

第一版每次切换都要重新扫 zip，第二版建议加入内存级缓存。

建议先做两层缓存：

1. `ZIP 扫描缓存`
2. `文件解析结果缓存`

缓存键建议：

- `zip_path`
- `zip_mtime`
- `inner_path`
- `file_size`

缓存目标：

- 切换 APP 时不重复扫全包
- 反复点同一个文件时不重复解析
- 大 zip 的交互明显更顺滑

### 3.3 结果展示增强

第二版 UI 不用大改风格，但要提升可读性。

建议补：

- 文件类型过滤
- 搜索框
- 解析状态标签
- 原始路径复制
- 错误信息分区展示
- `sqlite` 表切换展示

### 3.4 `sqlite` 能力增强

第一版只做表结构和前 20 行预览，第二版可以补这几项：

- 表列表导航
- 字段名搜索
- 行数限制选择
- 单元格长文本折叠/展开
- `wal/shm` 检测状态显示
- 查询失败时给更清晰错误

仍然不建议第二版做完整 SQL 控制台。

### 3.5 `mmkv` 二阶段能力

第二版不一定要完全做完 `mmkv` 字段级解析，但至少要把它从“黑盒文件”提升为“可操作对象”。

建议第二版达到：

- 识别 `mmkv` 文件分组
- 显示 key 文件和 crc 文件配对关系
- 支持导出原始 `mmkv` 文件
- 预留 `mmkv parser` 接口

如果时间足够，再研究：

- 是否接入现成 `mmkv` 解码方案
- 是否只支持部分常见纯字符串/数字场景

### 3.6 发布与工程化

第二版需要开始考虑“别人也能用”：

- 统一版本号策略
- `README` 补完整启动说明
- GitHub Actions 双平台构建
- Release 产物归档
- 错误日志与调试说明

## 4. 第二版的产品形态

第二版建议让界面更接近“分析器”而不是“demo 页面”。

推荐界面结构：

1. 顶部工具栏
2. 左侧 APP 列表
3. 中间文件列表
4. 右侧结果详情
5. 底部状态栏

顶部工具栏建议有：

- 选择 ZIP
- 重新扫描
- 搜索
- 导出当前结果
- 导出当前 APP

底部状态栏建议显示：

- 当前 ZIP
- APP 数量
- 候选文件数量
- 缓存命中状态
- 当前错误提示

## 5. 第二版的数据流建议

第一版数据流偏直接调用，第二版建议整理成更稳定的状态流。

建议流程：

```text
选择 ZIP
-> 建立扫描缓存
-> 生成 APP 索引
-> 生成候选文件索引
-> 用户选择文件
-> 命中文件解析缓存 / 执行解析
-> 更新结果面板
-> 用户导出结果
```

建议前端状态拆分：

- `zipState`
- `appState`
- `fileState`
- `parseState`
- `cacheState`
- `exportState`

## 6. 第二版 Rust 层建议拆分

第一版逻辑主要集中在 `lib.rs`，第二版建议开始拆模块。

推荐结构：

```text
src-tauri/src/
├─ commands/
│  ├─ scan.rs
│  ├─ parse.rs
│  └─ export.rs
├─ scanner/
│  ├─ zip_index.rs
│  ├─ path_rules.rs
│  └─ app_index.rs
├─ parser/
│  ├─ plist.rs
│  ├─ json.rs
│  ├─ sqlite.rs
│  └─ mmkv.rs
├─ cache/
│  ├─ scan_cache.rs
│  └─ parse_cache.rs
├─ models/
├─ error/
└─ lib.rs
```

这样做的原因：

- 第二版功能增多后，`lib.rs` 会迅速膨胀
- `scan / parse / export / cache` 拆开后更适合测试
- 为第三版继续扩展留空间

## 7. 第二版前端建议拆分

前端第二版建议开始组件化。

推荐拆成：

- `ZipToolbar`
- `AppListPanel`
- `FileListPanel`
- `ResultPanel`
- `StatusBar`

同时增加一个轻量状态层：

- 不一定上很重的状态管理库
- 可以先用 `React Context + hooks`
- 如果状态变复杂，再考虑 `zustand`

## 8. 第二版重点命令

Rust 命令层建议扩成下面这些：

- `scan_zip`
- `list_apps`
- `list_files`
- `parse_file`
- `export_file_result`
- `export_app_result`
- `get_cache_status`
- `clear_cache`

其中第二版新增重点是：

- `export_file_result`
- `export_app_result`
- `get_cache_status`
- `clear_cache`

## 9. 导出格式建议

第二版导出建议统一成稳定 JSON 格式。

导出结构建议：

```json
{
  "version": "2",
  "sourceZip": "...",
  "batchRoot": "...",
  "appId": "...",
  "innerPath": "...",
  "fileType": "...",
  "parseStatus": "ok",
  "parsedData": {},
  "meta": {}
}
```

如果是 APP 级导出，则外层增加：

- `apps`
- `files`
- `exportTime`

## 10. 缓存策略建议

第二版缓存先不要做磁盘缓存，优先做内存缓存。

原因：

- 实现简单
- 失效逻辑更清晰
- 足够支撑第二版体验提升

建议缓存策略：

- 扫描缓存：LRU 或单 ZIP 最近一次缓存
- 解析缓存：按文件粒度缓存最近结果
- 重新选择 zip 后自动切换缓存空间

后续第三版再考虑磁盘持久缓存。

## 11. `mmkv` 研究结论

第二版对 `mmkv` 的目标不应定太满。

更合理的第二版研究方向是：

1. 先识别哪些 APP 路径下存在 `mmkv`
2. 先建立 `mmkv` 文件清单模型
3. 先支持导出原文件
4. 调研是否存在稳定的 Rust 解析方案

结论建议：

- 第二版可以“支持管理 `mmkv` 文件”
- 第二版不强求“完全解析 `mmkv` 内容”

## 12. 第二版错误处理建议

第二版要把错误从“开发者看得懂”变成“用户也能看懂”。

建议每个错误同时包含两层信息：

1. 用户提示
2. 调试信息

例如：

- 用户提示：`这个 sqlite 文件无法读取，可能缺少 wal/shm 或文件损坏`
- 调试信息：`sqlite_open_failed: ...`

前端展示时：

- 默认显示用户提示
- 提供“查看详细错误”折叠区

## 13. 第二版性能建议

第二版不要过度优化，但需要避免明显卡顿。

建议：

- 扫描 zip 时只索引，不读取内容
- 文件解析按需触发
- 长 JSON 结果懒渲染
- 大 `sqlite` 只取预览
- 导出时后台执行并回传状态

## 14. 第二版测试重点

第二版测试建议聚焦在 4 类：

### 14.1 缓存测试

- 同一 zip 多次切换 APP
- 同一文件重复解析
- zip 更新后缓存失效

### 14.2 导出测试

- 单文件导出
- 单 APP 导出
- 导出失败路径处理

### 14.3 `sqlite` 测试

- 普通 sqlite
- `sqlite + wal + shm`
- 超长文本字段

### 14.4 UI 交互测试

- 扫描后自动选中
- 文件过滤
- 错误提示展示

## 15. 第二版 CI 建议

第二版开始建议正式做：

- `macos-latest` 构建
- `windows-latest` 构建
- 前端构建检查
- Rust 测试
- 样本 ZIP 回归检查

建议至少产出：

- macOS 测试构建
- Windows 测试构建
- zip 样本导出结果

## 16. 第二版实施顺序

推荐顺序如下：

### P0

- 扫描缓存
- 解析缓存
- 单文件导出
- APP 级导出
- README 修整

### P1

- 文件搜索/过滤
- `sqlite` 展示增强
- 错误提示优化
- 状态栏

### P2

- `mmkv` 文件管理
- `clear_cache`
- CI 双平台打包

## 17. 第二版验收标准

满足下面这些，就可以认为第二版达标：

1. 同一个 ZIP 重复切换 APP 无明显卡顿
2. 同一个文件重复打开能命中缓存
3. 可以导出当前文件解析结果
4. 可以导出当前 APP 的解析结果
5. `sqlite` 结果可切表查看
6. 遇到错误时界面提示明确
7. README 能让别人独立跑起来
8. 能产出基本的双平台构建结果

## 18. 我对第二版的建议结论

如果第一版已经“没啥问题”，第二版最值得做的不是马上冲 `mmkv` 全解析，而是按下面顺序推进：

1. 先做导出
2. 再做缓存
3. 再做结果面板增强
4. 再做 `sqlite` 展示增强
5. 最后做 `mmkv` 二阶段研究

这个顺序最稳，也最适合你后面演示和继续迭代。
